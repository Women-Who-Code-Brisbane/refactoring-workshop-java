apply plugin: 'java'

sourceCompatibility = 1.8
version = '1.0'
jar {
    manifest {
        attributes 'Implementation-Title': 'Java codebase for the refactoring workshop',
                   'Implementation-Version': version
    }
}

repositories {
    mavenCentral()
}